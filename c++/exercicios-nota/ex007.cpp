#include <iostream>
#include <math.h>
using namespace std;

int main() {
    float r, vol;
    cout << "Quanto vale o raio da esfera? ";
    cin >> r;
    vol = 3.14 * r * 2;
    cout << "O volume vale " << vol << endl;
}
